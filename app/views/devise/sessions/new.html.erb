<% provide(:title, "Sign in") %>
<h1>ログイン</h1>

<%= form_for(:session, url: user_session_path) do |f| %>
<div class="row">
  <div class="col-md-6 col-md-offset-3">

    <div class="field">
      <%= f.label :メールアドレス %>
      <%= f.text_field :email %>
    </div>

    <div class="field">
      <%= f.label :パスワード %>
      <%= f.password_field :password %>
    </div>

    <div class="actions">
      <%= f.submit "ログイン", class: "btn btn-large btn-primary" %>
    </div>
 
    <p>無料会員登録 <%= link_to "こちら", new_user_registration_path %></p>
  </div>
<% end %>